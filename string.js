//string: in javascript string are immutable. thats mean string are not change.
/*
let str = "saimon";
console.log(str.length); //length print
console.log(str[0]);   //single index letter print 


// Template literals: A way to have embedded expression in string.

let string = `this is the template literals.`;
let string = `this is the template literals ${2+2+3}`;
console.log(string);

// for ex:
let product = {
    brandname : "Ct Ball Pen",
    rating : 5,
    isDeal : true,
    price : 270,
    discount : 5,
};
console.log(`brand name ${product.brandname}, rating ${product.rating}, price ${product.price}`);


// Scape characters: \n for new line, \t for tap/space. 
*/